// Cloudinary config for the GATE Sandton Worship Team Hub.
// Not a traditional secret (meant to run in the browser) — see README for
// the tradeoff of using an unsigned upload preset.
export const cloudinaryConfig = {
  cloudName: "c8puxadc",
  uploadPreset: "worship-hub-resources"
};
