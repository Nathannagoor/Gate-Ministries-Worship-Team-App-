// Copy this file to firebase-config.js and fill in the values from your
// Firebase project (Project settings -> General -> Your apps -> Web app).
//
// Note: these values are not secret. Firebase web config is meant to be
// public (it ships in every visitor's browser). Real security comes from
// firestore.rules and storage.rules, not from hiding this file.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
